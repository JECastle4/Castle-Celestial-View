/**
 * Feature flags for experimental or work-in-progress features
 */

export const FEATURE_FLAGS = {
  // Venus UI is disabled while API integration is being completed
  // and before redesigning the info panel to be extensible to all planets
  VENUS_UI_ENABLED: false,
} as const;
